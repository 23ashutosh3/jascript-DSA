// 3. Group words by their first letter

// Input: ["apple", "ant", "banana", "ball"]
// Output: { a: ["apple","ant"], b:["banana","ball"] }


// 3. Group words by their first letter
// Input: ["apple", "ant", "banana", "ball"]
// Output: { a: ["apple","ant"], b:["banana","ball"] }

// 4. Check if two strings are anagrams

// Use a Map to count characters in both strings.

// Input: "listen", "silent"
// Output: true

// 5. Count frequency of elements in an array
// Input: [1,2,2,3,3,3]
// Output: {1:1, 2:2, 3:3}

// 6. Find element that appears only once

// All others appear twice.

// Input: [1,2,2,3,1]
// Output: 3

// 7. Find intersection of two arrays using Map
// Input: [1,2,3,4], [3,4,5,6]
// Output: [3,4]

// 8. Find the most frequent element in an array
// Input: [1,3,2,3,4,3,5]
// Output: 3

// 9. Group anagrams together
// Input: ["eat", "tea", "tan", "ate", "nat", "bat"]
// Output: [["eat","tea","ate"],["tan","nat"],["bat"]]

// 10. Find duplicate elements in an array
// Input: [1,2,3,4,2,3]
// Output: [2,3]

// 11. Count words in a sentence
// Input: "this is a test this is only a test"
// Output: { this:2, is:2, a:2, test:2, only:1 }

// 12. Check if two arrays have the same elements (order doesn’t matter)
// Input: [1,2,3], [3,2,1]
// Output: true

// 13. Find elements appearing more than n/3 times
// Input: [1,2,3,1,1,2,2,2]
// Output: [1,2]

// 14. Find missing numbers from 1 to N
// Input: [1,2,4,6]
// Output: [3,5]

// 15. Check if any two numbers add up to a target (Two Sum problem)
// Input: [2,7,11,15], target = 9
// Output: [0,1]

// 16. Find common characters in all strings
// Input: ["bella","label","roller"]
// Output: ["e","l","l"]

// 17. Longest substring without repeating characters
// Input: "abcabcbb"
// Output: 3  // ("abc")

// 18. Find words that appear exactly once
// Input: ["apple","banana","apple","orange"]
// Output: ["banana","orange"]

// 19. Check if two strings are isomorphic

// Each character in one string maps to exactly one in the other.

// Input: "egg", "add" → true
// Input: "foo", "bar" → false

// 20. Find the index of first unique element in an array
// Input: [2,3,4,2,3,5]
// Output: 2  // element 4